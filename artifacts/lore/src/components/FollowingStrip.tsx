/**
 * FollowingStrip — removed as part of follow-mechanism removal.
 * Follow lists no longer exist; discovery happens through listening behaviour.
 */
export function FollowingStrip() {
  return null;
}
